name: SkylliaPrestige
main: pl.b2t.skylliaprestige.SkylliaPrestige
version: ${version}
api-version: 1.21.4
folia-supported: true
authors:
  - 2b2t.pl
dependencies:
  server:
    Skyllia:
      load: BEFORE
      required: true
      join-classpath: true
    VaultUnlocked:
      load: BEFORE
      required: false
      join-classpath: true
commands:
  skyprestige:
    description: "Prestiz wyspy"
    aliases: ["prestiz", "isprestige"]
